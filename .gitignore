node_modules
.env
.env.*
.expo

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.swp
*~

# Secrets — NEVER commit these
Config.swift
*.xcconfig.local
secrets/

# Claude Code — personal settings (shared settings.json IS committed)
.claude/settings.local.json
.claude/worktrees/

# Reference text files (keep out of repo)
*.txt

# Binary docs that shouldn't be in source control
*.pdf
*.rtf
